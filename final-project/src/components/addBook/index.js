import React, { Component } from 'react';
import Modal from './modal';

class Post extends Component {
    render() {
      return (
        <div>
            <Modal/>
        </div>
      );
    }
  }
  
  export default Post;